interface:
  display_name: "InfoQ Backend Smoke Test"
  short_description: "Help with InfoQ backend smoke and cluster checks"
  default_prompt: "Use $infoq-backend-smoke-test to run backend HTTP smoke or two-node WebSocket cluster smoke after my backend changes."
