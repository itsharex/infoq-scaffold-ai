--- a/.agents/skills/infoq-version-bump/SKILL.md
+++ b/.agents/skills/infoq-version-bump/SKILL.md
@@ -1,6 +1,6 @@
 ---
 name: infoq-version-bump
-description: 在本仓库内统一升级项目版本，覆盖后端 Maven revision、前端 package 版本、Docker 镜像标签、发布文档与版本断言。适用于升级版本号、发布到指定 x.y.z、同步 README/docker/pom/package 版本等仓库级版本升级需求。默认策略保持现有 SQL 初始化文件名不变，除非用户明确提出独立 SQL 文件改名任务。
+description: 在本仓库内统一升级项目版本，覆盖后端 Maven revision、前端 package / 小程序 manifest 版本、Docker 镜像标签、发布文档与版本断言。适用于升级版本号、发布到指定 x.y.z、同步 README/docker/pom/package/manifest 版本等仓库级版本升级需求。默认策略保持现有 SQL 初始化文件名不变，除非用户明确提出独立 SQL 文件改名任务。
 ---
 
 # Infoq 版本升级
@@ -33,6 +33,9 @@
 - `infoq-scaffold-backend/infoq-core/infoq-core-bom/pom.xml`
 - `infoq-scaffold-frontend-react/package.json`
 - `infoq-scaffold-frontend-vue/package.json`
+- `infoq-scaffold-frontend-weapp-react/package.json`
+- `infoq-scaffold-frontend-weapp-vue/package.json`
+- `infoq-scaffold-frontend-weapp-vue/src/manifest.json`
 - `README.md` version badge
 - `doc/docker-compose-deploy.md`
 - `script/docker/docker-compose.yml`

--- a/.agents/skills/infoq-version-bump/references/targets.md
+++ b/.agents/skills/infoq-version-bump/references/targets.md
@@ -10,6 +10,13 @@
   - 顶层 `"version"`
 - `infoq-scaffold-frontend-vue/package.json`
   - 顶层 `"version"`
+- `infoq-scaffold-frontend-weapp-react/package.json`
+  - 顶层 `"version"`
+- `infoq-scaffold-frontend-weapp-vue/package.json`
+  - 顶层 `"version"`
+- `infoq-scaffold-frontend-weapp-vue/src/manifest.json`
+  - `"versionName"`
+  - `"versionCode"`（按 `x.y.z` 去掉点号生成，例如 `2.1.0 -> 210`）
 - `README.md`
   - `![Version](https://img.shields.io/badge/Version-x.y.z-...)`
 - `doc/docker-compose-deploy.md`

--- a/.agents/skills/infoq-version-bump/scripts/bump_version.sh
+++ b/.agents/skills/infoq-version-bump/scripts/bump_version.sh
@@ -11,9 +11,9 @@
 Usage: bump_version.sh [--dry-run] [--repo-root <path>] <x.y.z>
 
 Examples:
-  bump_version.sh 2.0.3
-  bump_version.sh --dry-run 2.0.3
-  bump_version.sh --repo-root /path/to/infoq-scaffold-ai 2.0.3
+  bump_version.sh 2.1.0
+  bump_version.sh --dry-run 2.1.0
+  bump_version.sh --repo-root /path/to/infoq-scaffold-ai 2.1.0
 EOF
 }
 
@@ -44,7 +44,7 @@
 replace_perl() {
   local file="$1"
   local expr="$2"
-  TARGET_VERSION="${TARGET_VERSION}" perl -0pi -e "${expr}" "${file}"
+  TARGET_VERSION="${TARGET_VERSION}" TARGET_VERSION_CODE="${TARGET_VERSION_CODE}" perl -0pi -e "${expr}" "${file}"
 }
 
 while [[ $# -gt 0 ]]; do
@@ -78,6 +78,7 @@
 }
 
 [[ "${TARGET_VERSION}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]] || fail "target version must match x.y.z, got: ${TARGET_VERSION}"
+TARGET_VERSION_CODE="${TARGET_VERSION//./}"
 
 require_command perl
 require_command grep
@@ -89,6 +90,9 @@
 BOM_POM="${REPO_ROOT}/infoq-scaffold-backend/infoq-core/infoq-core-bom/pom.xml"
 REACT_PACKAGE="${REPO_ROOT}/infoq-scaffold-frontend-react/package.json"
 VUE_PACKAGE="${REPO_ROOT}/infoq-scaffold-frontend-vue/package.json"
+WEAPP_REACT_PACKAGE="${REPO_ROOT}/infoq-scaffold-frontend-weapp-react/package.json"
+WEAPP_VUE_PACKAGE="${REPO_ROOT}/infoq-scaffold-frontend-weapp-vue/package.json"
+WEAPP_VUE_MANIFEST="${REPO_ROOT}/infoq-scaffold-frontend-weapp-vue/src/manifest.json"
 README_FILE="${REPO_ROOT}/README.md"
 DEPLOY_DOC="${REPO_ROOT}/doc/docker-compose-deploy.md"
 COMPOSE_FILE="${REPO_ROOT}/script/docker/docker-compose.yml"
@@ -102,6 +106,9 @@
   "${BOM_POM}"
   "${REACT_PACKAGE}"
   "${VUE_PACKAGE}"
+  "${WEAPP_REACT_PACKAGE}"
+  "${WEAPP_VUE_PACKAGE}"
+  "${WEAPP_VUE_MANIFEST}"
   "${README_FILE}"
   "${DEPLOY_DOC}"
   "${COMPOSE_FILE}"
@@ -142,6 +149,9 @@
   "${BOM_POM#${REPO_ROOT}/}" \
   "${REACT_PACKAGE#${REPO_ROOT}/}" \
   "${VUE_PACKAGE#${REPO_ROOT}/}" \
+  "${WEAPP_REACT_PACKAGE#${REPO_ROOT}/}" \
+  "${WEAPP_VUE_PACKAGE#${REPO_ROOT}/}" \
+  "${WEAPP_VUE_MANIFEST#${REPO_ROOT}/}" \
   "${README_FILE#${REPO_ROOT}/}" \
   "${DEPLOY_DOC#${REPO_ROOT}/}" \
   "${COMPOSE_FILE#${REPO_ROOT}/}" \
@@ -157,6 +167,10 @@
 replace_perl "${BOM_POM}" 's{<revision>[^<]+</revision>}{<revision>$ENV{TARGET_VERSION}</revision>}'
 replace_perl "${REACT_PACKAGE}" 's{("version"\s*:\s*")[^"]+(")}{$1$ENV{TARGET_VERSION}$2}'
 replace_perl "${VUE_PACKAGE}" 's{("version"\s*:\s*")[^"]+(")}{$1$ENV{TARGET_VERSION}$2}'
+replace_perl "${WEAPP_REACT_PACKAGE}" 's{("version"\s*:\s*")[^"]+(")}{$1$ENV{TARGET_VERSION}$2}'
+replace_perl "${WEAPP_VUE_PACKAGE}" 's{("version"\s*:\s*")[^"]+(")}{$1$ENV{TARGET_VERSION}$2}'
+replace_perl "${WEAPP_VUE_MANIFEST}" 's{("versionName"\s*:\s*")[^"]+(")}{$1$ENV{TARGET_VERSION}$2}'
+replace_perl "${WEAPP_VUE_MANIFEST}" 's{("versionCode"\s*:\s*")[^"]+(")}{$1$ENV{TARGET_VERSION_CODE}$2}'
 replace_perl "${README_FILE}" 's{(!\[Version\]\(https://img\.shields\.io/badge/Version-)[0-9]+\.[0-9]+\.[0-9]+(-[^)]*\))}{$1$ENV{TARGET_VERSION}$2}'
 replace_perl "${DEPLOY_DOC}" 's{(当前文档对应项目基线版本为 `)[^`]+(`。)}{$1$ENV{TARGET_VERSION}$2}'
 replace_perl "${COMPOSE_FILE}" 's{(image:\s+infoq/infoq-admin:)[0-9]+\.[0-9]+\.[0-9]+}{$1$ENV{TARGET_VERSION}}'
@@ -171,6 +185,10 @@
 assert_contains_fixed "${BOM_POM}" "<revision>${TARGET_VERSION}</revision>" "bom revision"
 assert_contains_fixed "${REACT_PACKAGE}" "\"version\": \"${TARGET_VERSION}\"" "react package version"
 assert_contains_fixed "${VUE_PACKAGE}" "\"version\": \"${TARGET_VERSION}\"" "vue package version"
+assert_contains_fixed "${WEAPP_REACT_PACKAGE}" "\"version\": \"${TARGET_VERSION}\"" "weapp react package version"
+assert_contains_fixed "${WEAPP_VUE_PACKAGE}" "\"version\": \"${TARGET_VERSION}\"" "weapp vue package version"
+assert_contains_fixed "${WEAPP_VUE_MANIFEST}" "\"versionName\": \"${TARGET_VERSION}\"" "weapp vue manifest versionName"
+assert_contains_fixed "${WEAPP_VUE_MANIFEST}" "\"versionCode\": \"${TARGET_VERSION_CODE}\"" "weapp vue manifest versionCode"
 assert_contains_fixed "${README_FILE}" "![Version](https://img.shields.io/badge/Version-${TARGET_VERSION}-" "README version badge"
 assert_contains_fixed "${DEPLOY_DOC}" "当前文档对应项目基线版本为 \`${TARGET_VERSION}\`。" "deploy doc baseline version"
 assert_contains_fixed "${COMPOSE_FILE}" "image: infoq/infoq-admin:${TARGET_VERSION}" "admin image tag"

--- a/.agents/skills/infoq-version-bump/scripts/test_bump_version.sh
+++ b/.agents/skills/infoq-version-bump/scripts/test_bump_version.sh
@@ -38,6 +38,9 @@
   "infoq-scaffold-backend/infoq-plugin/infoq-plugin-doc/src/test/java/cc/infoq/common/doc/config/properties/SpringDocPropertiesTest.java"
   "infoq-scaffold-frontend-react/package.json"
   "infoq-scaffold-frontend-vue/package.json"
+  "infoq-scaffold-frontend-weapp-react/package.json"
+  "infoq-scaffold-frontend-weapp-vue/package.json"
+  "infoq-scaffold-frontend-weapp-vue/src/manifest.json"
   "sql/infoq_scaffold_2.0.0.sql"
 )
 
@@ -51,6 +54,10 @@
 assert_contains_fixed "${FIXTURE_ROOT}/infoq-scaffold-backend/infoq-core/infoq-core-bom/pom.xml" "<revision>9.9.9</revision>"
 assert_contains_fixed "${FIXTURE_ROOT}/infoq-scaffold-frontend-react/package.json" "\"version\": \"9.9.9\""
 assert_contains_fixed "${FIXTURE_ROOT}/infoq-scaffold-frontend-vue/package.json" "\"version\": \"9.9.9\""
+assert_contains_fixed "${FIXTURE_ROOT}/infoq-scaffold-frontend-weapp-react/package.json" "\"version\": \"9.9.9\""
+assert_contains_fixed "${FIXTURE_ROOT}/infoq-scaffold-frontend-weapp-vue/package.json" "\"version\": \"9.9.9\""
+assert_contains_fixed "${FIXTURE_ROOT}/infoq-scaffold-frontend-weapp-vue/src/manifest.json" "\"versionName\": \"9.9.9\""
+assert_contains_fixed "${FIXTURE_ROOT}/infoq-scaffold-frontend-weapp-vue/src/manifest.json" "\"versionCode\": \"999\""
 assert_contains_fixed "${FIXTURE_ROOT}/README.md" "![Version](https://img.shields.io/badge/Version-9.9.9-"
 assert_contains_fixed "${FIXTURE_ROOT}/doc/docker-compose-deploy.md" "当前文档对应项目基线版本为 \`9.9.9\`。"
 assert_contains_fixed "${FIXTURE_ROOT}/script/docker/docker-compose.yml" "image: infoq/infoq-admin:9.9.9"

--- a/.agents/skills/infoq-version-bump/agents/openai.yaml
+++ b/.agents/skills/infoq-version-bump/agents/openai.yaml
@@ -1,4 +1,4 @@
 interface:
   display_name: "InfoQ Version Bump"
-  short_description: "Help with InfoQ Version Bump tasks"
-  default_prompt: "Use $infoq-version-bump to bump versions consistently across backend, frontend, docker, and release docs."
+  short_description: "Help with InfoQ version bumps across backend, admin, weapp, docker, and docs"
+  default_prompt: "Use $infoq-version-bump to bump versions consistently across backend, admin frontend, weapp frontend, docker, and release docs."
